<%= form_for(@meeting_user) do |f| %>
  <% if @meeting_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting_user.errors.count, "error") %> prohibited this meeting_user from being saved:</h2>

      <ul>
      <% @meeting_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meeting_id %><br>
    <%= f.number_field :meeting_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
